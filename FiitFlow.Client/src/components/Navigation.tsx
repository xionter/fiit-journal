export const rootMain = { to: "/StudentSubjects", options: { replace: true } };

export const rootEdit = { to: `${rootMain.to}/ConfigEditing`, options: { replace: true } };

export const rootLogin = { to: "/Login", options: { replace: true } };
